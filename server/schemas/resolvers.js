const {Book,User} = require('../models');

const resolvers = {
    Query: {
        //TBD
    },
    Mutation: {
        //LOOK AT user-controller.js as reference. 
    }
}

module.exports = resolvers;